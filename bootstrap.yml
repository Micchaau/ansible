- hosts: web
  gather_facts: false
  tasks:
    - wait_for_connection:
        timeout: 60
        delay: 10
        connect_timeout: 10

    - authorized_key:
        user: root
        key: "{{ lookup('file', '/root/.ssh/id_ed25519.pub') }}"
