<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <title>{{ inventory_hostname }}</title>
  <style>
    body {
      margin: 0;
      font-family: system-ui, sans-serif;
      background: #09090b;
      color: #fafafa;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }
    .card {
      background: #0c0c0f;
      border: 1px solid #27272a;
      border-radius: 12px;
      padding: 24px 28px;
      width: 420px;
    }
    h1 {
      margin: 0 0 16px;
      font-size: 20px;
    }
    .row {
      display: flex;
      justify-content: space-between;
      padding: 8px 0;
      border-top: 1px solid #27272a;
      font-size: 14px;
    }
    .row:first-child { border-top: none; }
    .label { color: #a1a1aa; }
  </style>
</head>
<body>
  <div class="card">
    <h1>WEB NODE</h1>

    <div class="row">
      <div class="label">Hostname</div>
      <div>{{ ansible_hostname }}</div>
    </div>

    <div class="row">
      <div class="label">Inventory</div>
      <div>{{ inventory_hostname }}</div>
    </div>

    <div class="row">
      <div class="label">Public IP</div>
      <div>{{ ansible_default_ipv4.address }}</div>
    </div>

    <div class="row">
      <div class="label">OS</div>
      <div>{{ ansible_distribution }} {{ ansible_distribution_version }}</div>
    </div>

    <div class="row">
      <div class="label">Kernel</div>
      <div>{{ ansible_kernel }}</div>
    </div>

    <div class="row">
      <div class="label">CPU</div>
      <div>{{ ansible_processor_vcpus }} vCPU</div>
    </div>

    <div class="row">
      <div class="label">Memory</div>
      <div>{{ ansible_memtotal_mb }} MB</div>
    </div>

    <div class="row">
      <div class="label">Uptime</div>
      <div>
        {{ (ansible_uptime_seconds // 3600) }}h
        {{ (ansible_uptime_seconds % 3600 // 60) }}m
      </div>
    </div>

    <div class="row">
      <div class="label">Deployed</div>
      <div>{{ ansible_date_time.date }} {{ ansible_date_time.time }}</div>
    </div>
  </div>
</body>
</html>
